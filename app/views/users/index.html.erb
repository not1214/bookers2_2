<div class="container">
  <div class="row">
    <div class="col-3">
    
      <h2>User Info</h2>

      <table class="table">
        <tr>
          <%= attachment_image_tag current_user, :profile_image, :fill, 30, 30, fallback: "no_image.jpg" %>
        </tr>
        <tr>
          <th>name</th>
          <th><%= current_user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th><%= current_user.introduction %></th>
        </tr>
      </table>

      <div>
      <%= link_to edit_user_path(current_user) do %>
        <button class="btn btn-block btn-outline-secondary"><i class="fas fa-user-cog"></i></button>
      <% end %>
      </div>
      
      <h2>New Book</h2>

      <%# <%= form_with model:@book, books_path, local:true do |f| %> 

      <%# <div class="field">
        <%= f.label :Title %>
        <%# <%= f.text_field :title, class: "form-control" %> 
      <%# </div> %>

      <%# <div class="field">
        <%= f.label :Opinion %>
        <%# <%= f.text_area :body, class: "form-control" %> 
      <%# </div> %>
<%#  %>
      <%# <div class="actions"> %>
        <%# <%= f.submit "Create Book", class: "btn btn-success btn-sm" %> 
      <%# </div> %> 
    <%# </div> %>
    </div>
    
    <div class="col-8 offset-1">
      <h2>Users</h2>
      <table class="table table-hover">
        <thead>
          <tr>
            <th>image</th>
            <th>name</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
        <% @users.each do |user| %>
          <tr>
            <th><%= attachment_image_tag user, :profile_image, :fill, 10, 10, fallback: "no-image-icon.jpg" %></th>
            <th><%= user.name %></th>
            <th><%= link_to "Show", user_path(user) %></th>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>


  </div>
</div>
